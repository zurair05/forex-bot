[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install flask flask-cors yfinance pandas numpy gunicorn"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT --workers 2 --timeout 120 server:app"
